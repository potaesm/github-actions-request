name: Cron request example
on:
  schedule:
    - cron: "* * * * *"
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: potaesm/github-actions-request
        with:
          url: ${{ secrets.URL }}
          method: get