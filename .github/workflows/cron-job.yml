name: Cron request example
on:
  push:
    branches: [ master ]
  schedule:
    - cron: "* * * * *"
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - uses: potaesm/github-actions-request
        with:
          url: ${{ secrets.URL }}
          method: get